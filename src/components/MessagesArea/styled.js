import styled from 'styled-components';

export const MessagesWrapper = styled.div`
  padding: 0 3rem;
  height: 70vh;
  overflow-y: scroll;
  position: relative;
`;

export const ConversationStartedAt = styled.div`
  padding: 1rem 0 0;
  text-align: center;
`;
export const ScrollButton = styled.div`
  position: fixed;
  padding-top: 1rem;
  z-index: 100;
`;
